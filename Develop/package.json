{
  "name": "googlebooks-app",
  "version": "1.0.0",
  "description": "A fully functioning Google Books API search engine built with a RESTful API, and refactor it to be a GraphQL API built with Apollo Server. The app was built using the MERN stack, with a React front end, MongoDB database, and Node.js/Express.js server and API. It's already set up to allow users to save book searches to the back end.",
  "main": "server/server.js",
  "engines": {
    "node": "15.x"
  },
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "Wiliam Wettering",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sirwettering/MERN-Book-Search.git"
  },
  "bugs": {
    "url": "https://github.com/sirwettering/MERN-Book-Search/issues"
  },
  "homepage": "https://github.com/sirwettering/MERN-Book-Search#readme",
  "dependencies": {
    "apollo": "^2.33.9",
    "apollo-server-express": "^3.6.2",
    "faker": "^6.6.6",
    "graphql": "^16.3.0",
    "jwt-decode": "^3.1.2",
    "mongodb": "^4.3.1",
    "nodemon": "^2.0.15"
  }
}
